# Optional: computer vision (server-side), e.g. frame processing alongside the web app.
# Install: pip install -r requirements-vision.txt
# Then download model: powershell -File scripts/download_hand_model.ps1
opencv-python-headless==4.10.0.84
mediapipe>=0.10.14
numpy>=1.24.0
pyttsx3>=2.90