# Requirements for Computer Vision are deprecated.
# Voice Ver Sign uses cloud-based multimodal APIs (e.g. Gemini API, WebSockets) and client-side processing,
# removing the need for server-side opencv, mediapipe, or local offline TTS.