# Pin versions for reproducible deploys — install inside a venv (see README).
fastapi==0.115.6
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
pydantic==2.10.3
python-dotenv==1.0.1
# PostgreSQL driver (optional if you only use SQLite)
psycopg2-binary==2.9.10
# MySQL 8.0 (XAMPP / Workbench) — use DATABASE_URL=mysql+pymysql://...
pymysql==1.1.1
# Used by Starlette/FastAPI TestClient and many integration tests
httpx==0.28.1

# Sign→Voice pipeline (MediaPipe hand landmarks, OpenCV frames, offline TTS)
opencv-python-headless==4.10.0.84
mediapipe>=0.10.14
numpy>=1.24.0
pyttsx3>=2.90
